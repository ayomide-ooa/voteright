const vote = () => {
    var votePoll = document.getElementById("votePoll")
        //votePoll.style.height += ''+1+'px'
        for(var x=1; x; x++) {
        console.log(`${x}px`)
        votePoll.style.height = `${x}px`
        }
        console.log(`${x}px`)
}